import sys
data = list(map(str.strip, sys.stdin))
print(data)
for j in data:
    spsk = []
    for i in j.split(' ')[1:-2:]:
        if j.split(' ')[0] < i and len(j.split(' ')[-1]) >= len(i):
            spsk.append(i)
    print(' & '.join(spsk))
